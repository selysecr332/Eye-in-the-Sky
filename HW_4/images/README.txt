Required for a strong HW_4 submission:

  opencv_detections.png   — live boxes + labels
  unreal_play_drone.png   — same mission, editor or game view
  terminal_connected.png — script log

Use consistent cropping and light annotations (arrows) sparingly.
